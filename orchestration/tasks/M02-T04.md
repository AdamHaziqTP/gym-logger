# M02-T04 — manual session-summary override UI

## Worker role

You are the bounded implementation worker for Gym Logger. Implement only this M02 task. Do not redefine product behavior, invent generic fitness features, or claim physical-iPhone or visual verification.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- M02-T01 History/search/editing, M02-T02 Copy Another Session, and M02-T03 whole-session deletion are accepted for automated scope.
- Product authority: `GYM_LOGGER_SPEC.md` §§4.1, 9.1–9.2, 12.3, 17, 20, 27 and the intentional seed mismatch (`40 sets · 39 exercises`).
- M01 FIX-05 physical-iPhone checks remain BLOCKED/DEFERRED on the iPhone 14 Pro Max LAN reachability result. Do not mark them passed.

## Objective

Make the session header totals manually editable without losing the distinction between calculated values and user overrides:

1. Preserve the existing calculated totals when no override exists.
2. Provide a clear, compact way to edit the displayed Sets and Exercises summary for the current session.
3. Preserve arbitrary user-entered display strings, including the intentional `40` / `39` mismatch and other free-form values; do not normalize them into a generic fitness model.
4. Persist changes through the existing local autosave path and keep them after leaving/reopening the session.
5. Provide a narrow reset-to-calculated action or equivalent so an override can be removed and calculated totals shown again.
6. Preserve the existing clone policy decision: source summary override is carried to a clone; new session row values/Skip/notes behavior remains unchanged.

## Scope constraints

- Reuse the existing session header and Dexie persistence patterns.
- Do not add a general settings system, analytics, backend, Notes integration, PNG export, backup/restore, or PWA installability.
- Do not alter the five visible highlight categories or deferred M01 human gate.
- Keep editing limited to summary totals; do not make the entire header or table read-only/editable indiscriminately.
- Choose the least disruptive interaction consistent with the current Notes-like UI and cover the exact behavior with tests.

## Acceptance criteria

- AC-01: The session header displays calculated totals when no override exists, including the seeded intentional 40/39 mismatch through existing data.
- AC-02: A user can edit Sets and Exercises display values independently and save arbitrary strings without silent normalization.
- AC-03: Override values persist after leaving and reopening the same session.
- AC-04: Reset-to-calculated removes the override and returns the displayed totals to calculated values.
- AC-05: Existing clone behavior preserves an existing summary override and does not blank Reps/Weight or alter row/Skip/notes policy.
- AC-06: Add focused component/data tests for edit, persistence/remount, reset, and arbitrary strings; all existing tests continue to pass.
- AC-07: Run `npm test -- --run --reporter=dot`, `npm run build`, and `git diff --check`.
- AC-08: Write `orchestration/reports/M02-T04.md` with files, commands/results, acceptance status, limitations, and questions. Explicitly state that physical-iPhone and visual acceptance were not performed.

## Completion rule

Do not claim M02 completion or M01 human-gate completion. If the spec does not determine a safe interaction detail, document the narrow ambiguity and stop only that branch; otherwise leave the working tree ready for Codex's independent review.
