# Codex review — M01-T01

## Decision

**AUTOMATED VERIFICATION PASSED — HUMAN VERIFICATION REQUIRED**

The M01 vertical slice is now a known-good automated checkpoint. Codex independently found and verified the lifecycle/test defects; OX repaired them through the infrastructure retry. Desktop evidence is complete for the automatable gate, but M01 is not product-accepted until the physical-iPhone and manual visual checks are recorded.

## OX invocations

1. Initial `M01-T01`: created the app tree but ran longer than 20 minutes without a report or clean exit; Codex stopped the runner. Independent verification found 22 failed tests and a TypeScript/runtime sort bug.
2. `M01-T01-FIX-01`: repaired the sort lookup and removed `src/tests/zz-debug.test.ts`; the worker again did not produce a report before the runner was stopped. Independent verification reached 29/32 passing tests and a passing build.
3. `M01-T01-FIX-02`: blocked at launch by `RATE_LIMIT: 429`, with no source changes.
4. `M01-T01-FIX-03`: infrastructure retry after quota recovery; fixed the shared-Dexie cleanup race, corrected the 41-row summary expectation, and awaited the asynchronous highlight write. OX reported 32/32 and build pass; Codex independently reran them.

## What is present

- React + TypeScript + Vite project.
- Dexie/IndexedDB session data layer.
- Seed import from the supplied 40-row fixture.
- Home → Start/Continue flow and idempotent local-date cloning.
- Free-form fixed five-column table, row-level color control, notes area, manual summary display.
- Unit/integration tests covering seed, clone, free-form persistence, and summary logic.

## Findings

- Build passes independently.
- Vite dev server and module routes respond successfully independently.
- `npm test -- --reporter=dot` passes 32/32 independently.
- The shared database remains usable through App remount; the persistence scenario passes without `OpenFailedError`.
- The summary test now preserves the specified simple-integer rule and expects 41.
- The color test waits for the asynchronous write and still asserts `purple`.
- Real iPhone offline, clipboard, PNG, and visual evidence are not available from this run.

## Product requirements covered by evidence

The implementation visibly addresses the first-slice intent for the seed fixture, free-form cells, five fixed columns, row-level colors, summary override display, local clone logic, and restrained dark table presentation. The automatable persistence/build/runtime contract is now green. Physical-device and manual visual acceptance remain open.

## Recommendation

Do not authorize M02 yet. Complete `orchestration/evidence/HUMAN-VERIFICATION-M01.md`, then record the human result. Keep rich Notes paste and tall PNG export deferred until their later milestones and real-iPhone testing are available.

## Human-gate rejection — 2026-08-24

The physical-iPhone pass rejected M01 despite the automated checkpoint. HV-02 and HV-03 passed: one current session was created and edits survived leaving/reopening. HV-04 failed because the handle exposed only colour, not the approved row menu/reorder interaction. HV-05 failed because the category legend was absent and the row treatment appeared heavier than the finalized §5.2 visual rule. A separate resume failure returned the user to Home instead of directly opening an existing current session.

This exposed a review-coverage defect: the prior automated gate checked row colour but did not assert the complete compound row command set or direct current-session resume. FIX-04 adds those command-level acceptance checks and keeps M02 blocked.

## FIX-04 Codex re-verification — 2026-08-24

Codex independently verified 58/58 tests, production build, LAN runtime smoke, diff hygiene, source hygiene, direct resume coverage, the full row-command suite, legend ordering, and the corrected per-cell visual treatment. The automated correction is a known-good checkpoint candidate. Physical iPhone touch/drag and visual acceptance remain open; the repeat checklist is `orchestration/evidence/HUMAN-VERIFICATION-M01-FIX-04.md`.

## Final human feedback — FIX-05

Adam approved the FIX-04 behavior with two narrow final changes: remove the visible `Other` legend item while retaining internal `none`, and prevent iOS Safari native text-selection/long-press behavior on the three-dot drag handle without disabling selection inside editable cells or Notes. M02 remains blocked pending FIX-05 and the final physical-iPhone gate.
