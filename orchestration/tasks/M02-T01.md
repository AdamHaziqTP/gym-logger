# M02-T01 — History, local search, and editable historical sessions

## Worker role

You are the bounded implementation worker for Gym Logger. Implement only this task. Do not redefine product behavior, invent generic fitness features, or claim physical-iPhone verification.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Base checkpoint: `f173046`
- Product authority: `GYM_LOGGER_SPEC.md` §§4.1, 4.3, 11, 17, 20, 27, and acceptance checks F1–F5.
- Current product process: M01 FIX-05 physical-iPhone checks are `BLOCKED/DEFERRED` because the temporary LAN build was unreachable on Adam's iPhone 14 Pro Max. This is not permission to mark them passed; it is permission to continue bounded engineering.
- The existing M01 app already stores complete `WorkoutSession` records in Dexie and renders a `SessionView` for a session id.

## Objective

Add the first M02 session-history slice:

1. Add a reachable History entry point from Home without removing the existing Today/Last Workout flows.
2. Show a reverse-chronological session list with dates and summary text, not inline tables.
3. Add one local search field. Search must match the display/local date, exercise text, Sets, Reps, Weight, Skip, and bottom notes using case-insensitive substring matching. Empty search shows all sessions.
4. Allow tapping a historical session to open the existing editable `SessionView` directly. Historical sessions are not read-only.
5. Preserve local-first behavior: no network, account, backend, analytics, or remote assets.
6. Keep current-session direct resume and all M01 row editing/highlight/persistence behavior intact.

## In scope

- A small Home → History → Session navigation path.
- History list component and its responsive dark Notes-like styling.
- Pure search/filter helper(s) with tests.
- History/session navigation tests.
- Any narrowly required data query helpers.
- Documentation/report updates needed for this task.

## Explicitly out of scope

- Copy Another Session preview/Use This Session flow; that is a later M02 task.
- Delete confirmation; that is a later M02 task.
- Summary override UI changes; preserve existing stored overrides and display behavior.
- Settings, full History filters, exercise history, autocomplete, Apple Notes clipboard, PNG export, backup/restore, PWA installability, or native packaging.
- Any change to the approved five visible highlight categories or the handle-only selection hardening.

## Acceptance criteria

- AC-01: Home has a usable History entry point and existing Today/Last Workout/current-session flows still work.
- AC-02: History lists every stored session in reverse chronological order, with date and summary only; it does not render every table inline.
- AC-03: Search is local, case-insensitive, and matches date, exercise, Sets, Reps, Weight, Skip, and notes text. Empty search restores the complete list.
- AC-04: Selecting a historical session opens the existing editable session view; changing a cell or notes persists to that historical session after reload/remount.
- AC-05: Existing M01 automated tests remain green, and new tests cover ordering, each required search surface, empty search, and historical navigation/edit persistence where practical.
- AC-06: `npm test` and `npm run build` pass. No network/backend is introduced.
- AC-07: Report changed files, commands/results, acceptance status, limitations, and any product question in `orchestration/reports/M02-T01.md`.

## Worker report format

Write `orchestration/reports/M02-T01.md` with:

- Summary
- Files changed
- Commands run and results
- Acceptance criteria status
- Known limitations and deferred M02 work
- Questions/concerns

Do not claim M01 human acceptance, M02 completion, physical-iPhone behavior, Apple Notes interoperability, or visual acceptance beyond what was actually tested.
