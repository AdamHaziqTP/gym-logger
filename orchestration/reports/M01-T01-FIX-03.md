# M01-T01-FIX-03 completion report

Worker: DSH/OX Alpha
Task: `orchestration/tasks/M01-T01-FIX-03.md` (infrastructure retry of the blocked FIX-02 correction)
Result: **COMPLETE** — all three required corrections applied; suite and build green.

## Files changed

1. `src/App.tsx`
   - Removed `db.close()` from the App effect cleanup. Closing during cleanup raced the autosaves that `SessionView` flushes on unmount, producing `DexieError: OpenFailedError` and lost edits during an App remount. The shared connection now stays usable through pending saves and the remount lifecycle; the bootstrap still reopens if a connection is externally closed (`db.isOpen()` guard retained). No persistence behavior weakened and save errors remain surfaced via the existing error handlers. The browser owns the page lifetime; tests close their own instances in `afterEach`.
2. `src/tests/persistence.test.ts`
   - Corrected the summary expectation from `42` to `41`: 38 rows still say `"1"` plus one row `"3"` = 41; `"8,6"` is not silently summed (spec §9.1 simple-integer rule). Comment updated accordingly; implementation unchanged.
3. `src/tests/App.test.tsx`
   - Row-highlight test now awaits the asynchronous colour write by polling IndexedDB with `waitFor` before asserting; the assertion that the row persists as `purple` is preserved unchanged.
   - Additional required test-only fix in the remount test (same test block as review finding 1): the final notes assertion used `getByDisplayValue("Cardio:\n- 15 min bike")`, which can never match because testing-library's default query normalizer collapses whitespace in the node value while comparing the raw matcher string. Passed an explicit `getDefaultNormalizer({ trim: false, collapseWhitespace: false })` so the assertion performs true exact equality on the free-form multi-line value — this strengthens rather than weakens the check and changes no product code.

## Commands and results

| Command | Result |
| --- | --- |
| `npm test` (baseline, before edits) | FAIL — 29 passed / 3 failed (matches Codex review exactly) |
| `npm test` (after edits) | PASS — 32/32 |
| `npm test` (repeat runs ×2) | PASS — 32/32 each; no flakiness observed |
| `npm run build` | PASS — tsc clean; Vite build output under `dist/` |

## Debug-artifact check

- No debug/diagnostic test files present (`zz-debug.test.ts` remained absent; a temporary diagnostic spec used to root-cause the last failure was deleted after use).
- No `debugger`, `console.log`, `TODO`, or `FIXME` residue anywhere under `src/`. Production logging remains limited to legitimate `console.error` failure handlers.
- Working tree contains only project files; nothing committed (implementation remains uncommitted per orchestration state).

## Verification notes

The temporary diagnostic run proved the corrected lifecycle end-to-end: with the close removed, cell edits and notes are present in IndexedDB immediately after unmount and after remount at every sampled checkpoint; the sole remaining failure was the normalizer mismatch described above.

## Limitations

- Real-iPhone offline behavior, Apple Notes archive flow, PNG export, and subjective visual acceptance were not verified here; they remain deferred per the task boundaries and M01 evidence ledger.
- This retry does not authorize M02 or the deferred rich clipboard/PNG features; M01 remains subject to human review.
