# M01-T01-FIX-05 — Final legend and iPhone drag-handle hardening

## Status

READY_FOR_OX

## Context

Read before editing:

- `GYM_LOGGER_SPEC.md` §§5.1-5.2, 7.2-7.5, 14.2, 21.5, 25.2, 26, and acceptance checks C2-C10.
- `orchestration/evidence/M01-final-feedback-2026-08-24.md`
- `orchestration/reviews/M01-T01.md`
- `orchestration/state/DECISIONS.md` and `REQUIREMENTS_MATRIX.md`
- The current `src/components/SessionView.tsx`, `src/domain/highlights.ts`, `src/styles.css`, and interaction tests.

This is the final bounded M01 human-feedback correction. Do not start M02, add Apple Notes/PNG export, or change application scope beyond these two items.

## Required corrections

### 1. Five-category visible legend

- The visible session header legend must contain exactly: `Arms`, `Back`, `Chest`, `Delts`, `Legs`, in that order.
- Remove the visible `Other` item.
- Retain `none` as the internal unhighlighted/white row state and keep the existing `None` option in the row colour control.
- Do not alter the locked highlight values or make unhighlighted/abs rows invalid.
- Update automated legend tests to assert exactly five entries and the exact order.

### 2. Harden only the three-dot drag handle against iOS native selection

When the user presses/holds/drags the three-dot handle:

- preserve normal tap → selection/menu behavior;
- preserve press/drag → reorder behavior and all existing data/position invariants;
- prevent native text selection, selection handles, long-press callout, and inappropriate browser drag behavior on the handle itself;
- use the narrowest handle-only CSS/event treatment (for example, the relevant `user-select`, `-webkit-user-select`, `-webkit-touch-callout`, `-webkit-user-drag`, `touch-action`, or context-menu prevention rules as appropriate);
- do not disable text selection globally or on `.cell-input`, editable cells, or `.notes-input`.

Add practical automated coverage by asserting the handle's relevant computed/source classes/styles/event prevention and preserving the existing tap/menu and pointer-reorder tests. Do not claim that jsdom proves iOS native long-press behavior; that remains a physical-device gate.

## Verification required from OX

- Run `npm test` and `npm run build`.
- Confirm no debug-only tests, diagnostics, or unrelated changes remain.
- Write `orchestration/reports/M01-T01-FIX-05.md` with changed files, results, and the limitation that real iOS long-press behavior still requires Adam.
- Do not claim M01 acceptance or authorize M02.
