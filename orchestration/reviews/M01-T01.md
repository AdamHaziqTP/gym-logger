# Codex review — M01-T01

## Decision

**REJECTED FOR HUMAN REVIEW**

The M01 vertical slice is substantially scaffolded, but it is not a known-good milestone. Codex independently found a real clone-path defect, which OX correction 1 repaired. A final independent run still has three reproducible failures, and the final OX correction was blocked by DSH rate limiting before it could make changes.

## OX invocations

1. Initial `M01-T01`: created the app tree but ran longer than 20 minutes without a report or clean exit; Codex stopped the runner. Independent verification found 22 failed tests and a TypeScript/runtime sort bug.
2. `M01-T01-FIX-01`: repaired the sort lookup and removed `src/tests/zz-debug.test.ts`; the worker again did not produce a report before the runner was stopped. Independent verification reached 29/32 passing tests and a passing build.
3. `M01-T01-FIX-02`: blocked at launch by `RATE_LIMIT: 429`, with no source changes.

## What is present

- React + TypeScript + Vite project.
- Dexie/IndexedDB session data layer.
- Seed import from the supplied 40-row fixture.
- Home → Start/Continue flow and idempotent local-date cloning.
- Free-form fixed five-column table, row-level color control, notes area, manual summary display.
- Unit/integration tests covering seed, clone, free-form persistence, and summary logic.

## Findings

- Build passes.
- Vite dev server and module routes respond successfully.
- `npm test -- --reporter=dot` fails 3 tests / passes 29.
- `src/App.tsx` closes the shared database during component cleanup; the App remount test demonstrates pending autosaves can fail with `OpenFailedError` and the edited text is lost.
- `src/tests/persistence.test.ts` expects 42 after one of 40 `Sets=1` rows becomes `3` and another becomes non-integer `8,6`; the correct calculated result is 41.
- The color test reads the database before awaiting the asynchronous color write.
- Real iPhone offline, clipboard, PNG, and visual evidence are not available from this run.

## Product requirements covered by evidence

The implementation visibly addresses the first-slice intent for the seed fixture, free-form cells, five fixed columns, row-level colors, summary override display, local clone logic, and restrained dark table presentation. It does not yet close the full M01 acceptance contract because persistence is not green.

## Recommendation

Do not authorize M02 or unattended continuation yet. Human review should either approve a targeted fix to the three remaining test/lifecycle issues, or reset to the last known-good handoff/orchestration checkpoint and rerun after the DSH quota resets. Keep rich Notes paste and tall PNG export deferred until real-iPhone testing is available.
