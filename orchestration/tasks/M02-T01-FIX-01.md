# M02-T01-FIX-01 — Complete History task evidence and coverage

## Worker role

You are correcting the bounded M02-T01 task. Preserve the existing History implementation unless a test exposes a genuine defect. Do not redesign the product or expand scope.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Current checkpoint: `662b164`
- Existing implementation: History entry point/list/search and historical-session route are already present in the working tree.
- Product authority: `GYM_LOGGER_SPEC.md` §§4.1, 4.3, 11, 17, 20, 27 and F1–F5.
- M01 physical-iPhone checks remain BLOCKED/DEFERRED on the iPhone 14 Pro Max LAN reachability result. Do not mark them passed.

## Objective

Finish M02-T01's evidence and coverage requirements after the first OX run timed out:

1. Review the existing History implementation for a real defect; make only narrowly necessary fixes.
2. Add practical component-level automated coverage for Home → History → selecting a historical session → returning to History, and historical session edit persistence where the existing test harness permits it.
3. Preserve the existing pure search tests and M01 behavior.
4. Run `npm test` and `npm run build`.
5. Write the required worker report to `orchestration/reports/M02-T01.md` with changed files, commands/results, AC-01 through AC-07 status, limitations, and questions.

## Out of scope

- Copy Another Session preview/Use This Session.
- Delete confirmation, summary override UI changes, Settings, Apple Notes, PNG export, backup/restore, PWA installability, native packaging, or M01 human-gate changes.
- Claiming physical-iPhone or visual acceptance.

## Acceptance criteria

- AC-01: Existing History behavior remains reachable from Home and current-session flows remain intact.
- AC-02: Reverse-chronological date/summary list remains covered by implementation/tests.
- AC-03: All required local search surfaces remain covered.
- AC-04: Component-level coverage exercises opening a historical session and the existing editable path; persistence coverage is added where practical.
- AC-05: Full existing and new tests pass.
- AC-06: Production build passes and no network/backend is introduced.
- AC-07: `orchestration/reports/M02-T01.md` is present and complete. Do not claim M02 completion or any human gate pass.
