# M01-T01-FIX-03 — Retry the unexecuted persistence/test correction

## Status

READY_FOR_OX

## Context

M01 remains unaccepted after the Codex review recorded in `orchestration/reviews/M01-T01.md`.
The previous FIX-02 invocation was stopped by a DSH/OpenRouter rate-limit response before OX executed any work or changed source. This task is therefore an infrastructure retry of the same correction, not a new implementation attempt and not permission to proceed to M02.

Work in the current Gym Logger project root. Read the existing M01 task, review, evidence, and source before editing. Preserve the finalized product/specification decisions.

## Required corrections

1. Fix the database lifecycle race in `src/App.tsx`.
   The component currently closes the shared Dexie instance from its effect cleanup. During an App remount, a pending autosave can race with that close and fail with `DexieError: OpenFailedError`, losing the edit. Keep the shared database usable through pending saves and the remount lifecycle. Do not weaken persistence behavior or hide save errors.

2. Correct the persistence test expectation in `src/tests/persistence.test.ts`.
   The fixture has 40 rows. Changing one Sets value from 1 to 3 and another to the non-numeric value `8,6` produces a calculated total of 41 under the specified numeric parsing behavior. Update the expectation and its comment to reflect the existing product rule; do not change the implementation merely to preserve 42.

3. Make the row-highlight test in `src/tests/App.test.tsx` await the existing asynchronous persistence before reading Dexie. Preserve the assertion that the selected row persists as `purple`; do not weaken or remove the assertion.

## Verification required from OX

- Run the complete automated test suite with `npm test`.
- Run `npm run build`.
- Confirm no debug-only test or diagnostic artifact is reintroduced.
- Write a concise completion report to `orchestration/reports/M01-T01-FIX-03.md`, including files changed, commands and results, and any limitation.

## Boundaries

- Do not implement M02 or deferred rich Apple Notes/PNG export features.
- Do not claim real-iPhone, Apple Notes, PNG, or subjective visual verification.
- Do not modify finalized handoff/product documents.
- If a genuine ambiguity or blocker prevents the correction, record it in the completion report and stop that branch; do not invent behavior.
