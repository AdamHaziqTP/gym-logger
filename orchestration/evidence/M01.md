# M01 evidence ledger

## Build

- `npm run build` — PASS. TypeScript and Vite production build completed; output was generated under `dist/`.

## Tests

- Final `npm test -- --reporter=dot` — FAIL: 29 passed, 3 failed out of 32.
- Remaining failures are reproducible in `src/tests/App.test.tsx` and `src/tests/persistence.test.ts`.
- The failures are not visual-only: pending edits can be lost when the shared Dexie database is closed during App remount, the highlight assertion does not wait for its async write, and one expected total is mathematically incorrect.

## Runtime

- `npm run dev -- --host 127.0.0.1 --port 4173` — PASS; Vite reported ready.
- `GET /` — HTTP 200, HTML root and `Gym Log` title present.
- `GET /src/main.tsx` — HTTP 200, JavaScript module served.

## Visual

- Codex inspected all seven supplied reference images and the produced `src/styles.css`/`SessionView.tsx` source.
- The source implements a near-black, subtle-grid, colored-text table with five fixed columns and compact mobile scrolling.
- A browser screenshot was not captured in this environment because Playwright/browser tooling is not installed or connected. The visual acceptance therefore remains unverified; real-iPhone visual/offline/Notes checks remain deferred by the product spec.

## Acceptance disposition

M01 is **NOT ACCEPTED**. The source tree is useful and the build passes, but the required persistence test is not green and the DSH correction budget is exhausted.
