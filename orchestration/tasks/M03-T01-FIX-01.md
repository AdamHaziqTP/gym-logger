# M03-T01-FIX-01 — recover clipboard copy on the target LAN/Safari path

## Worker role

You are the bounded correction worker for Gym Logger. This is the first correction attempt for M03-T01 after a real iPhone result. Do not claim Apple Notes compatibility or rich-paste success.

## Base state and human finding

- Project root: `C:\Users\adam4\gym_logger_handoff`
- M03-T01 engineering scope was independently green at 125/125 tests, but the product owner tested on an **iPhone 14 Pro Max** and pressing **Copy to Notes** showed `Copy failed — clipboard unavailable`.
- The app was served at `http://192.168.1.49:5173`.
- Evidence: `orchestration/evidence/HUMAN-VERIFICATION-M03-T01.md`.

## Investigation targets

Verify both likely contributors before choosing the fix:

1. Async Clipboard API availability/permission on a non-secure LAN HTTP origin (`navigator.clipboard`, `ClipboardItem`, `isSecureContext`) in the supported Safari path.
2. The current `Copy to Notes` handler awaits save flushing before starting the clipboard operation; iOS/Safari may require the clipboard call to remain inside the original user activation.

Do not silently reclassify this as a LAN reachability failure: the product owner reached the app and pressed the button.

## Required correction

Preserve rich `ClipboardItem` behavior where supported, but make the action have a best-effort plain-text path on the actual LAN/Safari conditions without claiming rich success:

- Keep the rich and Async Clipboard paths and truthful statuses intact for secure/capable browsers.
- Add a narrowly scoped legacy/plain fallback suitable for a direct button gesture when Async Clipboard is unavailable or denied (for example, a carefully scoped `document.execCommand("copy")` selection fallback), or document a concrete platform limitation if no safe browser fallback can work. Do not disable selection globally or alter editable table behavior.
- Avoid losing transient user activation by delaying the first clipboard attempt behind an unrelated awaited save. Preserve latest visible edits through a safe payload/save strategy and keep failure truthful.
- Restore any temporary selection/UI state and avoid copying unrelated page content.
- Add automated coverage for Async Clipboard unavailable/denied, legacy/plain fallback success, and the existing rich/plain/failure states. Do not weaken existing tests.

## Scope constraints

- Do not implement PNG export, Notes automation, RTF/native packaging, backend/cloud services, or generic sharing.
- Do not claim Apple Notes structure/colors survive paste.
- Do not change the five visible categories or the deferred M01 physical gate.
- If the LAN HTTP limitation cannot be solved safely in-browser, record it precisely and leave a human gate for a secure/reachable build rather than fabricating success.

## Acceptance criteria

- AC-01: On capable secure browsers, existing rich/plain behavior remains truthful.
- AC-02: On the tested LAN/Safari limitation, the app either reaches a truthful plain-copy fallback or clearly reports an unavoidable platform limitation with evidence; it must not incorrectly report rich success.
- AC-03: Clipboard invocation remains tied to the button gesture and does not regress table editing/autosave.
- AC-04: Automated tests cover all outcome paths and no unrelated selection behavior is changed.
- AC-05: Run `npm test -- --run --reporter=dot`, `npm run build`, and `git diff --check`.
- AC-06: Write `orchestration/reports/M03-T01-FIX-01.md` with diagnosis, files, results, limitations, and the next exact human checklist. Do not claim Apple Notes acceptance.
