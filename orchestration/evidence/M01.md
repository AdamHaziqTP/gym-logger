# M01 evidence ledger

## Build

- Initial review build — PASS. TypeScript and Vite production build completed; output was generated under `dist/`.
- Codex independent re-verification after `M01-T01-FIX-03` — PASS. `npm run build` completed cleanly; Vite generated the production bundle.

## Tests

- Initial review run — FAIL: 29 passed, 3 failed out of 32.
- OX `M01-T01-FIX-03` report — PASS: 32/32, repeated across three runs.
- Codex independent re-verification — PASS: 32/32 in 4 test files. This includes the remount persistence scenario, asynchronous row-highlight persistence, and the corrected free-form summary rule.

## Runtime

- `npm run dev -- --host 127.0.0.1 --port 4173` — PASS; Vite reported ready.
- Codex independent smoke: `GET /` — HTTP 200, HTML root and `Gym Log` title present.
- Codex independent smoke: `GET /src/main.tsx` — HTTP 200, JavaScript module served and contains `createRoot`.

## Visual

- Codex inspected all seven supplied reference images and the produced `src/styles.css`/`SessionView.tsx` source.
- The source implements a near-black, subtle-grid, colored-text table with five fixed columns and compact mobile scrolling.
- A browser screenshot was not captured in this environment because Playwright/browser tooling is not installed or connected. The visual acceptance therefore remains unverified; real-iPhone visual/offline/Notes checks remain deferred by the product spec.

## Codex audit

- `git diff --check` — PASS.
- No `zz-debug`, `debugger`, `console.log`, `TODO`, or `FIXME` residue under `src/`.
- `src/App.tsx` no longer closes the shared Dexie connection during component cleanup; test-owned connections remain explicitly closed.
- Product source and finalized handoff documents were not reorganized or overwritten.

## Acceptance disposition

All automatable M01 checks in this environment are **PASS**. M01 remains **NOT ACCEPTED** pending the human device/visual gate recorded in `orchestration/evidence/HUMAN-VERIFICATION-M01.md`.
