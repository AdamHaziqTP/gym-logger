# M02-T02 — Copy Another Session preview and clone flow

## Worker role

You are the bounded implementation worker for Gym Logger. Implement only this task. Do not redefine product behavior, invent generic fitness features, or claim physical-iPhone verification.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Base checkpoint: `41a42de`
- M02-T01 History/search/editing is independently accepted for automated scope. Reuse its History data/search behavior where appropriate.
- Product authority: `GYM_LOGGER_SPEC.md` §§4.1, 4.2, 4.3, 12, 17, 20, 27 and acceptance checks A1–A4.
- M01 FIX-05 physical-iPhone checks are BLOCKED/DEFERRED on the iPhone 14 Pro Max LAN reachability result. Do not mark them passed.

## Objective

Implement the M02 Copy Another Session flow:

1. Add a reachable `Copy Another Session` entry point from Home.
2. Show the existing reverse-chronological History/search picker.
3. Tapping a source session opens a compact read preview with its date and summary, not an editable table.
4. `Use This Session` creates today's session from the selected source using the existing clone policy: fresh session/row ids, exact row order/text/highlights, cleared Skip and notes, and preserved summary override according to `DEFAULT_CLONE_POLICY`.
5. Open the new today's session immediately and keep the one-session-per-local-date rule.
6. If today's session already exists, warn before replacing/creating another. The warning must prevent silent duplication; the chosen behavior and exact text must be covered by tests and documented in the worker report.

## In scope

- Home Copy Another Session entry point.
- Reusable source picker/search integration.
- Read preview and Use This Session action.
- Safe today-session conflict warning/confirmation.
- Clone integration and focused tests.

## Explicitly out of scope

- Whole-session deletion/confirmation (M02-T03).
- Summary override UI parity, Settings, Apple Notes, PNG export, backup/restore, PWA installability, native packaging, or M01 gate changes.
- Sticky search persistence, advanced filters, or inline full-table previews.

## Acceptance criteria

- AC-01: Home exposes `Copy Another Session` without removing Today, Last Workout, History, or current-session behavior.
- AC-02: Source selection is reverse chronological and searchable through the existing local search surfaces.
- AC-03: Preview is read-oriented, shows source date and summary, and has a clear `Use This Session` action without rendering an inline editable workout table.
- AC-04: Use creates exactly one today's session with fresh ids, cloned row order/text/highlights, cleared Skip/notes, and preserved summary override; it opens immediately.
- AC-05: Existing today's-session presence cannot silently create a duplicate; the warning/confirmation behavior is explicit and tested.
- AC-06: Existing tests plus new clone-flow tests pass; `npm test` and `npm run build` pass with no network/backend.
- AC-07: Write `orchestration/reports/M02-T02.md` with files, commands/results, acceptance status, limitations, and questions. Do not claim M02 completion, human acceptance, physical-iPhone behavior, or visual acceptance.
