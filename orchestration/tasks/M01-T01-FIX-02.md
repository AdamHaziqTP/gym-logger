# M01-T01-FIX-02 — Close the remaining M01 verification gaps

## Worker role

This is the final automatic correction attempt for M01. Make only the evidence-based changes below. Do not add features or rewrite tests to conceal product failures.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Prior correction: `orchestration/tasks/M01-T01-FIX-01.md`
- Evidence: `orchestration/reports/M01-T01-FIX-01-invocation.md`

## Findings to address

1. `src/App.tsx` closes the shared Dexie instance in the App effect cleanup. During a legitimate remount/reload-style lifecycle, `SessionView` flushes pending autosaves while the parent cleanup closes the database, producing `OpenFailedError` and losing the edited value. Keep the database available through the pending save/reopen lifecycle; the real browser owns the page lifetime. Preserve test cleanup behavior and do not remove autosave.
2. `src/tests/persistence.test.ts` expects `42` after changing one of 40 fixture rows from `1` to `3` and another to non-integer `8,6`. The correct calculated total is `41`; update the expectation/comment to reflect the spec's “sum simple integer Sets only” rule.
3. `src/tests/App.test.tsx` checks the async row-highlight write immediately after the click. Make the test await the observable persisted value (for example, `waitFor`) so it verifies the asynchronous local write deterministically. Do not weaken the assertion or change the product color semantics.

## Required verification

- `npm test` must pass.
- `npm run build` must pass.
- Do not leave diagnostic tests, debug logging, or temporary files.
- Write `orchestration/reports/M01-T01-FIX-02.md` with changed files, commands/results, and limitations.

## Scope boundary

No History/search, rich clipboard, PNG export, backup/restore, PWA manifest/service worker, native packaging, or product decisions. Those remain deferred milestones.
