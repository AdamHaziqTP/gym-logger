# M03-T11 hosted build report — run 32990405957

Status: `FAILED — CORRECTION APPLIED`

The macOS/Xcode runner built the web bundle and reached Swift compilation but
failed before packaging with exit code 65. Xcode 15.4 reported that
`PHAssetCreationRequest.creationRequestForAsset()` is obsoleted and must use
the Swift-imported `PHAssetCreationRequest.forAsset()` API.

Codex applied that narrow API correction and updated the matching static
contract assertion. No product behavior or Photos permission scope changed.
Local tests, build, one-app checks, native checks, and diff hygiene were
rerun successfully before the replacement hosted attempt.
