# M01-T01-FIX-01 — Repair the evidenced clone-path failure

## Worker role

You are a fresh implementation worker correcting the prior M01-T01 result. Make the smallest evidence-based repair. Do not rewrite tests to conceal the failure and do not broaden scope.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Prior implementation base: `dd727629269390e0acb774ff26d4b67380e51f62`
- Original task: `orchestration/tasks/M01-T01.md`
- Failure evidence: `orchestration/reports/M01-T01-ox-invocation.md`

## Root cause

`src/data/clone.ts` imports `sortSessionsNewestFirst(sessions)` from `src/data/db.ts`, but the latest-session lookup currently calls the local two-argument comparator `sortNewestFirst(array)[0]`. This causes the runtime error `Cannot read properties of undefined (reading 'dateLocal')`, and TypeScript reports the invalid call.

## Required correction

1. Change the latest-session lookup to use the imported array helper, or otherwise correctly sort the complete array before selecting `[0]`.
2. Keep the existing comparator use for sorting the date query, or simplify it without changing behavior.
3. Remove the unfinished `src/tests/zz-debug.test.ts`; it is diagnostic residue, not a product regression test.
4. Preserve all supplied fixture data, free-form fields, clone policy, idempotency, and existing acceptance tests.
5. Write `orchestration/reports/M01-T01-FIX-01.md` with files changed, commands/results, and limitations.

## Verification required from the worker

- `npm test`
- `npm run build`
- If either fails for a reason beyond this correction, report it exactly and do not mask it.

## Out of scope

No new features, no History/search, no rich clipboard, no PNG export, no backup/restore, no native packaging, no product decision changes.
