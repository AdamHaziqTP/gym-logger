# M03-T01-FIX-02 — preserve category text/highlight colors in Apple Notes rich paste

## Worker role

You are the bounded correction worker for Gym Logger. This is the second correction attempt for M03-T01. Do not redesign the app or claim Apple Notes color acceptance without a new iPhone paste result.

## Base state and human finding

- Project root: `C:\Users\adam4\gym_logger_handoff`
- Trusted HTTPS rich-paste retest on an iPhone 14 Pro Max succeeded for the editable table and all values.
- Human finding: the pasted result had no category text/highlight colors.
- Evidence: `orchestration/evidence/HUMAN-VERIFICATION-M03-T01.md`.
- Current HTML payload puts category `background-color` and `color` on the `<tr>` element in `src/domain/notesExport.ts`. Apple Notes appears to retain the table but strip that row-level styling.

## Objective

Make the smallest rich-payload compatibility correction that gives Apple Notes the best chance of preserving the existing five category colors:

1. Preserve the existing editable table structure, date/legend/summary/table/notes order, arbitrary values, summary override, and five-category taxonomy.
2. Encode category foreground/background styling on each table cell and/or an inline text wrapper inside each cell, rather than relying only on `<tr>` styling.
3. Use conservative inline/legacy-compatible color representation where appropriate (`style` on `td`/text and compatible HTML color attributes are acceptable); do not introduce external CSS, classes, scripts, images, RTF, native code, or generic sharing.
4. Keep unhighlighted/`none` rows white/uncolored and keep the visible legend exactly Arms, Back, Chest, Delts, Legs.
5. Keep the plain-text fallback unchanged and truthful.

## Automated acceptance criteria

- AC-01: Existing 140+ automated tests continue to pass.
- AC-02: Payload tests prove every colored row's cell/text markup carries the correct category foreground and background tokens, while `none` remains uncolored.
- AC-03: HTML remains escaped, deterministic, self-contained, and table-structured.
- AC-04: `npm test -- --run --reporter=dot`, `npm run build`, and `git diff --check` pass.
- AC-05: Write `orchestration/reports/M03-T01-FIX-02.md` with files, diagnosis, results, and an explicit iPhone retest checklist. Do not claim the colors now work on Apple Notes.

## Human-gate boundary

After Codex independently verifies the correction, repeat the secure HTTPS iPhone test. Record separately whether editable table structure, values, foreground text colors, and background/highlight colors survive. If Apple Notes still strips them, document that as platform evidence and stop for the next product/technical decision; do not silently downgrade the requirement.
