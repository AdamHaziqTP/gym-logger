# M02-T03 — whole-session deletion confirmation

## Worker role

You are the bounded implementation worker for Gym Logger. Implement only this M02 task. Do not redefine product behavior, invent generic fitness features, or claim physical-iPhone or visual verification.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- M02-T01 History/search/editing and M02-T02 Copy Another Session are accepted for automated scope.
- Product authority: `GYM_LOGGER_SPEC.md` §§4.1, 11.1–11.4, 17, 20, 27 and acceptance checks F1–F6.
- M01 FIX-05 physical-iPhone checks remain BLOCKED/DEFERRED on the iPhone 14 Pro Max LAN reachability result. Do not mark them passed.

## Objective

Add safe deletion of a whole app session while preserving the Apple Notes authority boundary:

1. A user can reach deletion for an existing Gym Log session from an appropriate existing History/session flow.
2. Deleting a whole session requires confirmation.
3. Use the specified copy: `Delete this session from Gym Log? This does not affect your Apple Notes archive.`
4. Cancel leaves the session and current navigation/data unchanged.
5. Confirm removes only the selected Gym Log session, returns to a sensible existing screen, and does not affect any other session or Apple Notes-related copy.
6. Do not add a recycle bin or whole-session undo requirement; row-level delete/undo is out of scope.

## Scope constraints

- Reuse existing History/session navigation and Dexie database patterns.
- Keep the one-session-per-local-date and local-only behavior intact.
- Do not implement summary override UI, Settings, Apple Notes, PNG export, backup/restore, or PWA installability in this task.
- Do not change the deferred M01 gate or visible five-category legend decision.
- Keep confirmation copy exact; placement may follow the least disruptive existing History/session affordance and must be covered by tests.

## Acceptance criteria

- AC-01: A historical or current app session has a discoverable whole-session delete action within the existing product flow.
- AC-02: Delete always requires explicit confirmation with the exact specified Apple Notes-safe wording.
- AC-03: Cancel/backdrop dismissal performs no deletion; the selected session remains intact.
- AC-04: Confirm deletes only the selected session, leaves other sessions intact, and returns to a stable existing screen without a stale deleted-session view.
- AC-05: The operation is local and transactional enough not to silently mutate unrelated records.
- AC-06: Add focused component/data tests covering confirmation, cancel, confirm, and preservation of another session; all existing tests continue to pass.
- AC-07: Run `npm test -- --run --reporter=dot`, `npm run build`, and `git diff --check`.
- AC-08: Write `orchestration/reports/M02-T03.md` with files, commands/results, acceptance status, limitations, and questions. Explicitly state that physical-iPhone and visual acceptance were not performed.

## Completion rule

Do not claim M02 completion or M01 human-gate completion. If the existing UI does not provide a non-ambiguous placement without product redesign, document the narrow issue and stop only that branch; otherwise leave the working tree ready for Codex's independent review.
