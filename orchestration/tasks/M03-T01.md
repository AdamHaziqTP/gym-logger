# M03-T01 — Apple Notes clipboard integration spike

## Worker role

You are the bounded implementation worker for Gym Logger. Implement only this M03 technical spike. Do not claim that Apple Notes rich structure or colors work without target-iPhone evidence.

## Base state

- Project root: `C:\Users\adam4\gym_logger_handoff`
- M02 History/search/editing, Copy Another Session, whole-session deletion, and summary override are accepted for automated scope.
- Product authority: `GYM_LOGGER_SPEC.md` §§2.2, 10, 12, 15, 17, 22, 27 and the existing supplied Notes screenshots/references.
- M01 FIX-05 physical-iPhone checks remain BLOCKED/DEFERRED on the iPhone 14 Pro Max LAN reachability result.

## Objective

Build the smallest real local clipboard spike for the existing session screen:

1. Add a clearly labeled Copy to Notes action in the existing session flow without redesigning the product.
2. Generate the current session's date, visible legend/categories, summary, fixed-column table values, and bottom notes in a deterministic export representation.
3. Attempt the platform clipboard path with HTML plus plain-text fallback where the browser permits; keep a plain-text fallback available and clearly report failure when clipboard permission/API is unavailable.
4. Preserve arbitrary strings, highlights/categories, Skip values, summary overrides, and Notes text in the generated representations.
5. Keep the operation local-only and avoid backend/cloud integrations.
6. Do not implement PNG export, automatic Apple Notes append, backup/restore, or native packaging in this spike.

## Human-gate boundary

The worker and Codex may verify deterministic payload generation, button behavior, error handling, and build/runtime availability. They must not mark the Apple Notes outcome accepted. After engineering acceptance, create a precise human checklist covering actual iPhone Safari → Apple Notes paste, surviving structure, text, colors/highlights, date/summary/table/notes order, and plain fallback behavior.

## Acceptance criteria

- AC-01: Existing session flow exposes a Copy to Notes action without changing table editing/navigation.
- AC-02: HTML and plain payloads are deterministic and preserve free-form cell values, Skip, summary overrides, row highlights, legend/category context, and bottom notes.
- AC-03: Clipboard API failure or denial does not silently claim success; the UI reports a useful local failure/fallback state.
- AC-04: Automated tests cover payload escaping/ordering, arbitrary strings, highlight/category output, and success/failure action behavior.
- AC-05: Run `npm test -- --run --reporter=dot`, `npm run build`, and `git diff --check`.
- AC-06: Write `orchestration/reports/M03-T01.md` with files, commands/results, payload limitations, and an explicit human Apple Notes gate. Do not claim rich paste acceptance.

## Scope constraints

- Do not infer that HTML clipboard support equals Apple Notes compatibility.
- Do not add PNG export, settings, cloud sync, analytics, or generic sharing flows.
- Do not alter the deferred M01 gate or the five visible highlight categories.
- If a product ambiguity affects payload content, document it and stop only that branch.
